structure Person {
    integer age;
    double_precision height;
}

integer createstudent(text studentname, integer studentage) {
    integer x;
    x <- 8;
    return x;
}

text krowa <- "krowa";
integer z <- 10;
integer y <- createstudent(krowa, z);
<= y;
Person janek;
janek.age <- y;